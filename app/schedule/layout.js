export const metadata = {
  title: "Schedule",
  description: "View the complete 3-day schedule for LITUMUN 2026 — committee sessions, workshops, and cultural events.",
};

export default function ScheduleLayout({ children }) {
  return children;
}
