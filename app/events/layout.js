export const metadata = {
  title: "Events",
  description: "Explore MUN committees, cultural events, and workshops at LITUMUN 2026.",
};

export default function EventsLayout({ children }) {
  return children;
}
